#include <omnirefl/reflection.hpp>

struct record {
  int value;
};

struct read_value {
  template <typename Binding>
  int operator()(Binding binding) const {
    return binding.value.value;
  }
};

int main() {
  return 23 == omni::reflected_call(read_value{}, record{23}) ? 0 : 1;
}
