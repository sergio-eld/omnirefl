#include <omnirefl/reflected_scope.hpp>

struct record {
  int value;
};

struct read_value {
  template <typename Binding>
  int operator()(Binding binding) const {
    return binding.ref().value;
  }
};

int reflected_value() {
  return omni::reflected_call(read_value{}, record{29});
}
